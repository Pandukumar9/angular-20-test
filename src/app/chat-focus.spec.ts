import { ChatFocus } from './chat-focus';

describe('ChatFocus', () => {
  it('should create an instance', () => {
    const directive = new ChatFocus();
    expect(directive).toBeTruthy();
  });
});
