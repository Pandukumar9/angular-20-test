import { ChatNotify } from './chat-notify';

describe('ChatNotify', () => {
  it('should create an instance', () => {
    const directive = new ChatNotify();
    expect(directive).toBeTruthy();
  });
});
